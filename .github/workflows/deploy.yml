name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.6.1

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install blog dependencies
        run: pnpm install
        working-directory: ./blog

      - name: Build blog
        run: pnpm run build
        working-directory: ./blog

      - name: Install works dependencies
        run: pnpm install
        working-directory: ./works

      - name: Build works
        run: pnpm run build
        working-directory: ./works

      - name: Prepare deploy artifact
        run: |
          mkdir -p deploy/blog deploy/works
          cp -r ./blog/dist/. deploy/blog/
          cp -r ./works/dist/. deploy/works/
          echo '<meta http-equiv="refresh" content="0; url=/blog/">' > deploy/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./deploy

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128

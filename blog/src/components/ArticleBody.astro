<article class="article-body">
	<slot />
</article>

<style>
	.article-body {
		overflow: hidden;
		padding: 3.75rem 2.75rem 4.5rem 4.75rem;
		border: 1px solid color-mix(in srgb, var(--text-secondary) 24%, transparent);
		border-radius: 2rem 2rem 0.35rem 0.35rem;
		background:
			linear-gradient(90deg, transparent 0 3.9rem, var(--accent-primary) 3.9rem calc(3.9rem + 1px), transparent calc(3.9rem + 1px)),
			repeating-linear-gradient(
				to bottom,
				transparent 0,
				transparent 3.05rem,
				color-mix(in srgb, var(--border) 16%, transparent) 3.05rem,
				color-mix(in srgb, var(--border) 16%, transparent) calc(3.05rem + 1px)
			),
			var(--surface);
		box-shadow: 0.45rem 0.7rem 0.55rem color-mix(in srgb, var(--border) 34%, transparent);
	}

	:global(.article-body h2) {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin: 0 0 2.25rem;
		color: var(--text-primary);
		font-family: var(--font-heading-1);
		font-size: var(--text-2xl);
		font-weight: 700;
		letter-spacing: 0;
		line-height: 1.3;
	}

	:global(.article-body h2:not(:first-child)) {
		margin-block-start: 3.5rem;
	}

	:global(.article-body h2::before) {
		width: 2.25rem;
		height: 1px;
		background: color-mix(in srgb, var(--text-secondary) 45%, transparent);
		content: '';
	}

	:global(.article-body h3) {
		margin: 3rem 0 1.25rem;
		padding-inline-start: 0.85rem;
		border-inline-start: 3px solid var(--accent-primary);
		color: var(--text-primary);
		font-family: var(--font-heading-1);
		font-size: var(--text-xl);
		font-weight: 700;
		letter-spacing: 0;
		line-height: 1.35;
	}

	:global(.article-body p) {
		margin: 0;
		color: var(--text-primary);
		font-family: var(--font-article-body);
		font-size: var(--text-lg);
		/* line-height: 2.25; */
	}

	:global(.article-body p + p) {
		margin-block-start: 0.5rem;
	}

	:global(.article-body code) {
		border-radius: 0.35rem;
		background: color-mix(in srgb, var(--text-secondary) 12%, transparent);
		font-family: var(--font-code);
		font-size: 0.9em;
	}

	:global(.article-body p code) {
		padding: 0.12em 0.35em;
	}

	:global(.article-body pre) {
		margin: 2rem 0;
		padding: 1.25rem 1.4rem;
		border: 1px solid color-mix(in srgb, var(--text-secondary) 20%, transparent);
		border-radius: 0.85rem;
		overflow-x: auto;
		box-shadow: inset 0 0 0 1px color-mix(in srgb, #ffffff 6%, transparent);
	}

	:global(.article-body pre code) {
		padding: 0;
		background: transparent;
		font-size: var(--text-sm);
		line-height: 1.8;
	}

	:global(.article-body img) {
		display: block;
		max-width: 100%;
		height: auto;
		margin: 1.5rem 0 2rem;
		border-radius: 0.75rem;
	}

	:global(.article-body blockquote) {
		margin: 2rem 0;
		padding: 1rem 1.25rem;
		border-inline-start: 4px solid var(--accent-primary);
		border-radius: 0.75rem;
		background: color-mix(in srgb, var(--accent-primary) 10%, var(--surface));
		color: var(--text-primary);
	}

	:global(.article-body blockquote p) {
		font-size: var(--text-md);
		line-height: 1.9;
	}

	@media (max-width: 900px) {
		.article-body {
			padding-inline: 3.5rem 1.5rem;
		}
	}

	@media (max-width: 720px) {
		.article-body {
			padding: 2.25rem 1.25rem 3rem 2.25rem;
			border-radius: 1.35rem 1.35rem 0.35rem 0.35rem;
			background:
				linear-gradient(90deg, transparent 0 1.7rem, var(--accent-primary) 1.7rem calc(1.7rem + 1px), transparent calc(1.7rem + 1px)),
				repeating-linear-gradient(
					to bottom,
					transparent 0,
					transparent 2.55rem,
					color-mix(in srgb, var(--border) 16%, transparent) 2.55rem,
					color-mix(in srgb, var(--border) 16%, transparent) calc(2.55rem + 1px)
				),
				var(--surface);
		}

		:global(.article-body h2) {
			gap: 0.75rem;
			font-size: var(--text-xl);
		}

		:global(.article-body h2::before) {
			width: 1.25rem;
		}

		:global(.article-body p) {
			font-size: var(--text-md);
			line-height: 2.1;
		}
	}
</style>
